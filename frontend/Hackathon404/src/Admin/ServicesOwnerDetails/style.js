import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  addIconStyle: {
    position: 'absolute',
    right: 60,
  },
  deleteIcon: {
    position: 'absolute',
    right: 10,
    top: 20,
  },
  editIcon: {
    position: 'absolute',
    right: 40,
    top: 20,
  },
});

export default styles;
